# «ПК ТУТ» — production runtime dependencies.
# Только то, что реально импортируется в backend/server.py и backend/tests/.
# Всё установимо с публичного PyPI без внутренних индексов.

# ---- Web framework ----
fastapi==0.110.1
uvicorn==0.25.0

# ---- MongoDB ----
motor==3.3.1
pymongo==4.6.3

# ---- Validation & config ----
pydantic==2.13.4
python-dotenv==1.2.2

# ---- HTTP client (используется для VK API notifications) ----
httpx==0.28.1

# ---- Dev / tests ----
pytest==9.1.1
requests==2.34.2
